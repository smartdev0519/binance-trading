const DELAY_TWO = 2500000000;
const DELAY_ONE = 500000000;

export const delayTwo = () => {
    let count = 0;
    while(count < DELAY_TWO) {
        count++;
    }
}

export const delayOne = () => {
    let count = 0;
    while(count < DELAY_ONE) {
        count++;
    }
}
